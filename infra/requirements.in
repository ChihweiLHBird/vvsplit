# Direct deployment dependencies. Regenerate the Linux x86_64 / Python 3.14
# hash lock used by GitHub Actions:
#   uv pip compile --python-version 3.14 \
#     --python-platform x86_64-manylinux_2_34 --only-binary=:all: \
#     --generate-hashes requirements.in -o requirements.txt
pulumi>=3.247.0,<4.0
pulumi-cloudflare>=6.17,<7.0
